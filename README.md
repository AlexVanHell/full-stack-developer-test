This is a microservices proyect of Parking control and management App

# Setup Database

Copy `mongo-root.example` into new file `mongo-root`

```sh
$ cp ./mongo-root.example ./mongo-root
```

Then change the values you want